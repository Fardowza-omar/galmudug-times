server {
    listen 80;
    server_name galmudug-times.com www.galmudug-times.com;

    # Cloudflare will handle SSL, so this block serves HTTP
    # If using Cloudflare Full (Strict) SSL, uncomment the SSL block below

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Upload size (matches multer 5MB limit)
        client_max_body_size 10M;
    }

    # Cache static files
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# Uncomment below for Cloudflare Full (Strict) SSL with origin certificate
# server {
#     listen 443 ssl;
#     server_name galmudugtimes.com www.galmudugtimes.com;
#
#     ssl_certificate /etc/ssl/cloudflare/origin.pem;
#     ssl_certificate_key /etc/ssl/cloudflare/origin-key.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         client_max_body_size 10M;
#     }
#
#     location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_set_header Host $host;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
# }
